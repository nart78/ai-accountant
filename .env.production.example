# Copy this to .env on your VPS and fill in the values
# NEVER commit this file to git

# Your Anthropic API key (get from console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Strong random password for the database
DB_PASSWORD=change-this-to-a-strong-password

# Strong random secret key (generate with: openssl rand -hex 32)
SECRET_KEY=change-this-to-a-random-secret

# Your GitHub username
GITHUB_USER=nart78

# Basic Auth — protects the site with a username/password
# Username (choose whatever you want, e.g. johnny)
BASIC_AUTH_USER=johnny
# Hashed password — generate on the VPS with:
#   docker run --rm caddy:2-alpine caddy hash-password
# Paste the output here (example hash below is NOT real, replace it)
BASIC_AUTH_HASH=$2a$14$REPLACE_THIS_WITH_REAL_HASH_FROM_CADDY_COMMAND
